import React from 'react';

const AdminDashboard = () => {
  return (
    <div className="card">
      <h2>Admin Dashboard</h2>
      <p>This is a minimal placeholder. Use the backend /api/admin routes for managing movies, events, offers & reports.</p>
    </div>
  );
};

export default AdminDashboard;
